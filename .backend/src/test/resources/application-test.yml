spring:
  datasource:
    url: jdbc:h2:mem:sportsecommerce;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

app:
  jwt:
    issuer: sports-ecommerce-test
    secret: testsecretkeyforintegrationtests123456
    access-token-expiry-seconds: 3600
    refresh-token-expiry-seconds: 1209600
