SERVER_PORT=8080

# Optional: unsplash | local | firebase — catalog fallbacks when no manifest entry exists.
# Product images are primarily driven by classpath product-image-manifest.json.
PRODUCT_IMAGE_SOURCE=local
DB_URL=jdbc:mysql://db-dev:3306/sports_ecommerce
DB_USERNAME=sports_dev_user
DB_PASSWORD=change_me
JWT_SECRET=replace_with_dev_secret_min_32_chars
